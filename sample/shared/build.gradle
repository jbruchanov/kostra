import com.jibru.kostra.plugin.ResourcesDefaults

plugins {
    id("org.jetbrains.kotlin.multiplatform")
    id("com.android.kotlin.multiplatform.library")
    id("org.jetbrains.compose")
    id("org.jetbrains.kotlin.plugin.compose")
    id("org.jetbrains.kotlin.native.cocoapods")
    id("com.jibru.kostra.resources")
}

apply from: "$rootProject.projectDir/../build-ktlint.gradle"
apply from: "$rootProject.projectDir/../build-android-lib.gradle"

def depModules = [project(":shared-lib1"), project(":shared-lib2")]

kotlin {
    jvm()
    android {
        namespace = "com.jibru.kostra.appsample"
    }
    cocoapods {
        version = "1.0.0"
        summary = "Some description for the Shared Module"
        homepage = "Link to the Shared Module homepage"
        ios.deploymentTarget = "14.1"
        podfile = project.file("../appIos/Podfile")
        framework {
            baseName = "shared"
            binaryOption("bundleId", "com.jibru.kostra.sample.shared")
            binaryOption("bundleVersion", "2")
            isStatic = true
        }
    }

    iosSimulatorArm64()

    applyDefaultHierarchyTemplate()

    sourceSets {
        commonMain {
            dependencies {
                implementation(libs.kostra.common)
                implementation(libs.kostra.compose)
                implementation(libs.bundles.jetbrains.compose.app)
                implementation(libs.jetbrains.compose.ui.tooling.preview)

                //api necessary for cocoapods export
                depModules.forEach {
                    api(it)
                }
            }
        }

        commonTest {
            dependencies {
                implementation(kotlin("test"))
            }
        }

        jvmMain
        androidMain

        iosMain {
            sourceSets {
                //add explicit reference to use module resources + kostra resources as part of this build
                //output in kostra/sample/build/ios/Debug-iphonesimulator/appIos.app/compose-resources/
                depModules.forEach { Project p ->
                    def srcResources = new File(p.projectDir, "src/commonMain/resources")
                    //KostraPluginConfig.outputAssetsDir() = <buildDir>/generated/kostra/assets — the
                    //per-target wiring in the kostra plugin adds this to each KMP target's resources
                    //for the OWNING module (so lib1's iosArm64ProcessResources picks it up), but KMP
                    //does NOT propagate a sub-project's iosMain.resources into a consumer's
                    //iosMain.resources. We pull each depModule's generated dir into shared's iosMain
                    //explicitly so the produced .framework actually contains lib1+lib2 kostra DBs.
                    //(Was previously `generated/kostra/resources`; renamed to `assets` to reflect that
                    //the same files feed Android's variant.sources.assets pipeline.)
                    def genResources = new File(p.layout.getBuildDirectory().get().asFile, "generated/kostra/assets")
                    //`srcResources` is a hand-authored source directory — if it's missing that's a
                    //genuine misconfiguration, so fail loudly.
                    if (!srcResources.exists()) {
                        throw new IllegalStateException("IOS shared depends on ${p.name} resources, but ${srcResources.absolutePath} doesn't exist!")
                    }
                    //`genResources` is a BUILD OUTPUT (produced by ${p.name}:generateDatabases). On a
                    //clean checkout — and during every Android Studio / IDE Gradle sync, which fetches
                    //the project model BEFORE running any task — this directory legitimately doesn't
                    //exist yet. Throwing here made sync fail with
                    //"IOS shared depends on shared-lib1 resources, but …/generated/kostra/resources
                    //doesn't exist!". Gradle tolerates a `srcDir` that points at a not-yet-created
                    //directory (it's treated as empty until populated), and the explicit
                    //`generateDatabases.dependsOn(:shared-lib1:generateDatabases, …)` at the bottom of
                    //this file guarantees it IS populated before any iOS resource packaging runs. So
                    //just register it unconditionally — no existence check needed.
                    resources.srcDirs(srcResources)
                    resources.srcDirs(genResources)
                }
            }
        }

    }
}

kostra {
    KClassName = "com.sample.app.K"
    interfaces = true
    resourcesDefaults.value(ResourcesDefaults.values().toList())
    //Sample resources intentionally exercise partial-translation cases
    //(`en-rUS` / `en-rGB` variants without a separate base `en.xml`), so
    //strict-mode coverage flags them as missing. Disable so the demo build
    //stays green — the production behaviour is tested via the kostra plugin's
    //own test suite, not via this sample.
    strictMode = false
    androidResources {
        painterGroups.add("flags.*")
        keyMapper { key, file -> key }
        //just to let plugin know about extra resources,
        //strings are transformed to own DB and don't need to be part of APK as xmls
        resourceDirs.add(file("src/commonMain/resources_strings"))
    }
}


//just ensure that lib DBs are created with 'shared' module DB
tasks.getByName("generateDatabases")
    .dependsOn(":shared-lib1:generateDatabases", ":shared-lib2:generateDatabases")

//Android Studio renders previews by instantiating composables against the
//Android target's runtime classpath. `ui-tooling` is the preview-runtime
//library (LayoutInflater stubs, preview-only CompositionLocals, etc.). With
//the AGP-9 `com.android.kotlin.multiplatform.library` plugin the right
//configuration is `androidRuntimeClasspath` (the older Android Gradle Plugin
//variant `debugImplementation` doesn't exist on the new plugin — see the
//"Compose UI Previews in KMP" doc). Guarded by `plugins.withId` so modules
//that opt into this convention but don't apply the new Android KMP plugin
//skip the wiring silently instead of failing at config time with
//"configuration not found".
plugins.withId("com.android.kotlin.multiplatform.library") {
    dependencies {
        androidRuntimeClasspath(libs.jetbrains.compose.ui.tooling)
    }
}
